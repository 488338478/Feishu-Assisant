# Handoff — 飞书团队 Agent（v2 重构完成，待认证修复与部署）

日期：2026-09-09 ｜ 项目：`C:\Users\48833\Desktop\Graduate\assistant`（Python 包名 `assistant`，**非 git 仓库**）

## 当前状态（已完成）

v2 架构重构全部完成并验证：自研 [TOOL]/[PLAN] 文本协议、关键词预取、bypassPermissions 已废弃（存档 `_legacy/`）。新架构 = 每条飞书消息一次 `claude -p --resume` 调用（`agent/runtime.py`），按 chat_id 切 profile（docs/thesis/dev，cwd 机制加载 CLAUDE.md + .claude/settings.json + skills），按 sender open_id 分三层权限（read/edit/submit，叠加 `--disallowedTools`）。Phase 3 功能也已提前落地：stream-json 进度反馈（handlers 进度消息节流原地编辑）、scheduler `agent_jobs` 定时 agent 任务（每周更新日志为配置项）、p4v skill（review/日志/bug 筛查/feature 工作流）、deploy 全套（env.example / assistant.service / README / setup.sh / requirements.txt）。

**先读这些，不要重复其中的内容：**
- 架构与权限模型：`assistant/ARCHITECTURE.md`（v2，含「已知限制与评估」表）
- 决策记录（5 项用户拍板）：`assistant/PLAN.md` §14
- 部署步骤：`assistant/deploy/README.md`
- 持久记忆（每次会话自动加载）：memory 目录下 `feishu-assistant-project.md`、`feishu-assistant-v2-architecture.md`、`feishu-bot-auth-issues.md`

## 验证结论（真实 claude 调用冒烟通过）

基本问答/session 续接 ✅；docs profile 越权拒绝（无文件工具、非白名单 Bash 被拒）✅；agent 自主 lark-cli 排错 ✅；stream-json 进度事件 ✅（stats 返回 tools/duration）；scheduler agent_jobs 配置加载与周调度计算 ✅。简单调用 12-18s，多步探索 ~80s。

## 阻塞项（只能用户做，下次会话大概率围绕这些展开）

1. `lark-cli --profile assistant-bot auth login` — 补 user 登录（`docs +search` 需要）+ 修复 app secret（当前报 10014 invalid）
2. 飞书后台**轮换 App Secret**（旧 secret 曾明文硬编码，视为泄露），配 `FEISHU_APP_ID`/`FEISHU_APP_SECRET` env
3. 本地启动验证：`python -m assistant.main`（从 `Graduate/` 目录跑），群里发「我的权限」「推送到这里」
4. 服务器部署：`deploy/setup.sh` + 填 `assistant.env`（含 `ANTHROPIC_API_KEY`，用户已拍板 API 按量）
5. dev 模式：`profiles/dev/` 拷入 P4 工作区、填 CLAUDE.md 的 3 处 TODO（引擎/目录结构/分支模型）、配 `p4 protect`
6. 从 `data/audit_log.jsonl` 取 open_id/chat_id 填 `data/runtime_config.json`（改即时生效，免重启）

## 环境要点（容易踩坑）

- Windows 11，PowerShell/Bash 均可用；`claude` 2.1.162 在 PATH；`lark-cli` profile 名 `assistant-bot`
- **本机访问不了 GitHub**（gitclone 镜像也 502）：lark 官方 skills 未装，各 profile 的 CLAUDE.md 手写速查表兜底；服务器上网络允许时 `npx skills add larksuite/cli -g -a claude -s '*' -y` 补装
- 运行数据在 `assistant/data/`（旧目录已按用户要求清理）；`runtime.run()` 返回 `(text, stats)` 二元组，不是单字符串
- 编辑 `ARCHITECTURE.md` 前需先 Read（曾被外部修改）；repo 无版本控制，大改前提醒用户考虑 `git init`

## 下一步会话建议

- 用户回来第一句大概是"认证修好了/启动报错了"——用 **verify** skill 驱动 `python -m assistant.main` 看实际行为；若报错用 **diagnose** skill 的复现→定位→验证循环
- 若进入服务器部署阶段：照 `deploy/README.md` 逐步执行，setup.sh 已自动化前 6 步
- 若用户要给 dev 群开 P4V：先确认 P4 protections 已配（`p4 protect`），再往 `runtime_config.json` 填映射，**先灰度一个群、先 read tier**
