# 文档评论实测记录（2026-09-12）

目标文档：[《封卦手记》总策划文档](https://ocnwy60j0tlz.feishu.cn/docx/D5HDdoga7oms85xTOmWcdSLDnub)。时间均为北京时间。

## 已确认的真实链路

使用用户已经提交的两条请求，核对本地审计、SQLite 投递状态，并通过飞书评论 API 回读机器人实际回复；浏览器页面也可见原线程的审阅结果。

| 场景 | 触发回复 ID | 机器人回复 ID | 结果 |
| --- | --- | --- | --- |
| 原线程中不带 @发送 `111` | `7684564245282884536` | `7684564850042735580` | 16:33:59 提交，16:36:20 回贴，约 141 秒；审计记录模型执行 137.3 秒。回复明确沿用前文“审视总体文章结构”的要求，给出结构审阅建议。 |
| 新建局部评论 @机器人“给点建议” | `7684564455404882902` | `7684565540232285152` | 16:34:48 提交，16:39:01 回贴，约 253 秒；审计记录模型执行 158.0 秒。内容针对该评论引用的 4.4.1 协同连击。 |

原线程 comment_id 为 `7684398553627626684`，新线程为 `7684564455384435673`。两条业务记录状态均为 `done`，分别使用各自 comment_id 构成的会话键；没有把新评论的回复投递到旧线程。

## “没反应”的原因

这两条请求均已收到并成功执行。同一文档的任务串行处理，第二条在前一条完成后才开始执行；执行本身需两分多钟，叠加排队后约四分多钟才出现最终回复。当前实现没有排队或处理中回执，因此等待期间没有可见反馈。此体验缺口尚未修复，不能把上述结果理解为延迟问题已经解决。

此前“请补充具体文字指令”则是另一处已修复的解析错误：飞书真实文本字段是 `text_run.text`，旧实现误读 `text_run.content`。现有解析优先读取 `text`，并保留旧格式兼容与真实结构回归用例。

## 验证边界

本轮核验的是用户实际触发的完整链路，并未成功代用户创建新的测试评论。尝试 CLI user 身份新增独立测试评论时，因缺少评论创建及元数据 OAuth scope 被拒绝；浏览器为未登录访客态。没有扩展授权、修改文档正文或伪造用户事件。

现有结果支持“新评论 @触发、原处回复、无 @追问可使用线程历史”。`111` 并非严格的记忆断言，随机代号跨轮复述、无 @新线程拒绝、多人插话及异常重投仍需独立实机验证。两条回复话题不同与会话键不同支持线程隔离，但不能替代全部串话边界测试。
